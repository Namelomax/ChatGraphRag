/**
 * Логическая схема полей протокола для финального документа (зеркало Hakaton ProtocolSchema).
 * Передаётся только в system промпта генератора артефакта — не в чат.
 */
export const PROTOCOL_DOCUMENT_STRUCTURE_PROMPT = `## ЦЕЛЕВАЯ СХЕМА ДОКУМЕНТА «ПРОТОКОЛ ОБСЛЕДОВАНИЯ»

При сборке финального текста соблюдай эту логическую структуру полей:

1) protocolNumber — номер протокола (формат вида «№…»)
2) meetingDate — дата встречи строго ДД.ММ.ГГГГ
3) agenda — объект: title (основная тема), items[] (пункты повестки)
4) participants — объект:
   - customer: organizationName, people[] { fullName, position }
   - executor: organizationName, people[] { fullName, position }
   Две стороны; для каждой — таблица ФИО и должность.
5) termsAndDefinitions — массив { term, definition }
6) abbreviations — массив { abbreviation, fullForm }
7) meetingContent — объект: introduction (опционально), topics[] { title, content, subtopics? }, migrationFeatures? (если уместно по расшифровке)
8) questionsAndAnswers — массив { question, answer }
9) decisions — массив { decision, responsible }
10) openQuestions — массив строк (нерешённые вопросы)
11) approval — executorSignature { organization, representative }, customerSignature { organization, representative }

Правила качества:
- Решения всегда с ответственным.
- Участники — полные ФИО и должности; организации заказчика и исполнителя явно.
- Пропуски помечай как «Информация не предоставлена в расшифровке», не выдумывай.
`;

/**
 * Фиксированный вид документа (порядок как в Hakaton `protocolToMarkdown`).
 * Не менять нумерацию и названия разделов — только содержимое.
 */
export const FORUS_PROTOCOL_FIXED_MARKDOWN_OUTLINE = `
Первая строка документа (ровно в этом виде, подставь номер):
ПРОТОКОЛ ОБСЛЕДОВАНИЯ №[номер — если в данных без «№», добавь префикс №]

Далее строго по порядку (нумерацию разделов 1–10 не переименовывать и не переставлять):

1.\tДата встречи: [ДД.ММ.ГГГГ]

2.\tПовестка: [основная тема]
Если есть пункты повестки — каждый с маркером «- » на новой строке.

3.\tУчастники:

**Со стороны Заказчика [полное название организации]:**

Таблица Markdown | ФИО | Должность | с разделителем | --- | --- |

**Со стороны Исполнителя [полное название организации]:**

Таблица Markdown | ФИО | Должность/роль | с разделителем | --- | --- |

4.\tТермины и определения:

Список строк вида: «термин – определение»

5.\tСокращения и обозначения:

Список строк вида: «сокращение – полная форма»

6.\tСодержание встречи:

Сначала абзац-вводная «В ходе встречи обсуждались следующие вопросы:», затем развернутое описание тем и содержания по данным из запроса.
При наличии подтем — вложенные списки. При наличии таблицы особенностей (вкладка / блок возможностей) — таблица | Вкладка | Особенности |.

7.\tВопросы:

Маркированный список вопросов (одна строка на вопрос).

После списка вопросов — три пустые строки, затем строка **Ответы**: и маркированный список ответов в том же порядке.

8.\tРешения:

Для каждого пункта: формулировка решения и строка «Ответственный: …».

9.\tОткрытые вопросы:

Нумерованный или маркированный список нерешённых вопросов.

10.\tСогласовано:

Таблица Markdown из двух колонок: «Со стороны Исполнителя | Со стороны Заказчика», затем строка организаций, затем строка подписей вида «[ФИО] /______________» для каждой стороны.
`;
